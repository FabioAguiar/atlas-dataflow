{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Atlas DataFlow Dataset Public Profile Draft",
  "description": "Admin-editable draft of the public presentation state for a dataset: curated display copy, home-card metadata, a bounded theme preset, a reference-only binding to the existing predict view customization surface, and result-card presentation copy. This schema is presentation state, not contract, model, metric, inference, release, snapshot, or publication authority. It must never carry runtime contract fields, trained model data, calculated metrics, inference bundle contents, registry/release identity beyond dataset_slug, or publication/visibility status -- those remain owned by their own canonical artifacts and are represented here, when needed, only as bounded references. dataset_slug is the primary identity and must match the registry slug pattern; it is not replaced or aliased by this contract. This schema defines the editable draft shape only -- publication into a public snapshot is a separate, later operation.",
  "type": "object",
  "required": ["schema_version", "dataset_slug"],
  "additionalProperties": false,
  "not": {
    "anyOf": [
      {"required": ["contract"]},
      {"required": ["runtime_contract"]},
      {"required": ["public_contract"]},
      {"required": ["model"]},
      {"required": ["model_card"]},
      {"required": ["metrics"]},
      {"required": ["features"]},
      {"required": ["validation_rules"]},
      {"required": ["inference_bundle"]},
      {"required": ["registry"]},
      {"required": ["release_manifest"]},
      {"required": ["active_release"]},
      {"required": ["snapshot"]},
      {"required": ["publishing"]}
    ]
  },
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "title": "Profile Schema Version",
      "description": "Semantic version of this profile draft schema structure in MAJOR.MINOR.PATCH format. Required. Independent from any other contract's schema_version."
    },
    "dataset_slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
      "minLength": 1,
      "title": "Dataset Slug",
      "description": "Primary identity of the dataset this draft profile applies to. Must match the registry slug pattern: lowercase ASCII letters, digits, and single hyphen separators. Carried from the registry -- do not encode local paths, private names, release details, or alternate routing semantics in this field."
    },
    "display": {
      "$ref": "#/definitions/display"
    },
    "home_card": {
      "$ref": "#/definitions/home_card"
    },
    "performance_focus": {
      "type": "object",
      "additionalProperties": false,
      "required": ["focus_id", "highlighted_score_id", "visible_scores"],
      "description": "Curated public performance presentation. Values are presentation-only and never replace canonical training metrics.",
      "properties": {
        "focus_id": {"type": "string", "enum": ["overall_discrimination", "positive_class_detection", "balanced_classification", "probability_quality", "operational_decision", "regression_performance"]},
        "highlighted_score_id": {"type": "string", "minLength": 1, "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$"},
        "visible_scores": {"type": "array", "minItems": 1, "maxItems": 10, "items": {"$ref": "#/definitions/performance_score"}}
      }
    },
    "theme": {
      "$ref": "#/definitions/theme"
    },
    "inference_presentation": {
      "$ref": "#/definitions/inference_presentation"
    },
    "result_card": {
      "$ref": "#/definitions/result_card"
    },
    "documentation": {
      "$ref": "#/definitions/documentation"
    },
    "_example_note": {
      "type": "string",
      "description": "Optional annotation for example or demonstration artifacts only. Omit in production profile draft records."
    }
  },
  "definitions": {
    "performance_score": {
      "type": "object",
      "additionalProperties": false,
      "required": ["score_id", "display_label", "value", "value_source", "order"],
      "properties": {
        "score_id": {"type": "string", "pattern": "^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$"},
        "display_label": {"type": "string", "minLength": 1, "maxLength": 64, "pattern": "^[A-Za-z0-9][A-Za-z0-9 @'().+/-]*$"},
        "value": {"type": "string", "minLength": 1, "maxLength": 32, "pattern": "^[+-]?(?:[0-9]+(?:\\.[0-9]+)?|\\.[0-9]+)(?:%|[eE][+-]?[0-9]+)?$"},
        "value_source": {"type": "string", "enum": ["canonical", "manual"]},
        "order": {"type": "integer", "minimum": 0, "maximum": 99}
      }
    },
    "display": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional curated public-facing copy for the dataset. All fields are presentation text or presentation-format labels only and must not carry validation rules, technical metadata, or business logic. This is admin-editable curation, distinct from -- and layered above -- the technical dataset context narrative.",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Optional curated public display title, overriding the default dataset title for presentation purposes only."
        },
        "subtitle": {
          "type": "string",
          "minLength": 1,
          "description": "Optional curated public subtitle."
        },
        "problem_summary_title": {
          "type": "string",
          "minLength": 1,
          "description": "Optional curated heading for the public problem-summary section."
        },
        "problem_summary_body": {
          "type": "string",
          "minLength": 1,
          "description": "Optional curated public-safe explanatory text for the problem-summary section. Must not restate or redefine the real problem_type or prediction target owned by technical artifacts."
        },
        "source_name": {
          "type": "string",
          "minLength": 1,
          "description": "Optional public-safe label naming the dataset's original source."
        },
        "source_url": {
          "type": "string",
          "format": "uri",
          "description": "Optional public-safe URL for the dataset's original source."
        },
        "release_date_label": {
          "type": "string",
          "anyOf": [
            {"format": "date", "pattern": "^\\d{4}-\\d{2}-\\d{2}$"},
            {"pattern": "^\\d{2}/\\d{2}/\\d{4}$"}
          ],
          "description": "Public release date projection. New writes materialize YYYY-MM-DD from the canonical Dataset Detail timestamp; the legacy display form remains readable for existing drafts."
        },
        "release_date_mode": {
          "type": "string",
          "enum": ["auto", "manual"],
          "description": "Deprecated compatibility field for existing drafts. New writes omit it and it has no date-authority semantics."
        },
        "date_format": {
          "type": "string",
          "enum": ["dd/mm/yyyy", "mm/dd/yyyy", "yyyy-mm-dd"],
          "description": "Optional selected display format for date-shaped labels in this profile. Constrained to a small closed set of supported formats."
        },
        "canonical_name_fallback": {
          "type": "boolean",
          "description": "Optional flag indicating whether the public experience should fall back to the canonical dataset name when no curated title is set."
        }
      }
    },
    "home_card": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional curated Home card presentation metadata. Fields are presentation-only and must not duplicate or redefine technical dataset identity, metrics, or model behavior.",
      "properties": {
        "icon": {
          "type": "string",
          "enum": [
            "telecom",
            "bank",
            "generic",
            "telecom-users",
            "bank-building",
            "chart-line",
            "heart",
            "shopping-cart",
            "airplane",
            "shield",
            "education-cap",
            "energy-bolt",
            "home-house",
            "agro-leaf",
            "logistics-truck",
            "factory",
            "weather-cloud",
            "database",
            "money-dollar",
            "globe",
            "flask",
            "cpu-chip"
          ],
          "description": "Optional selected Home card icon. \"telecom\", \"bank\", and \"generic\" are the original deterministic-fallback values and remain supported for backward compatibility with already-published data. The remaining values are Atlas's controlled curated icon bank, selectable by an authoring curator regardless of dataset domain; values outside this set are not renderable and must be rejected."
        },
        "background_image_ref": {
          "type": ["string", "null"],
          "minLength": 1,
          "maxLength": 256,
          "pattern": "^/media/[A-Za-z0-9_-]+(?:/[A-Za-z0-9_-]+)*\\.(?:avif|gif|jpeg|jpg|png|webp)$",
          "description": "Optional same-origin public media reference for a Home card background image. Only /media/... image paths are accepted; raw bytes, filesystem paths, traversal segments, private URLs, query strings, and fragments are rejected. Null when no background image is set."
        },
        "short_description": {
          "type": "string",
          "minLength": 1,
          "description": "Optional curated short public-safe description shown on the Home card."
        },
        "primary_metric_key": {
          "type": ["string", "null"],
          "minLength": 1,
          "description": "Optional reference to a metric name/key that may exist in the dataset's release metrics evaluation data, used to select which metric is highlighted on the Home card. This is a reference to a metric identifier only -- never a duplicated numeric metric value. Null when no primary metric is selected."
        }
      }
    },
    "theme": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional selected visual theme preset for the dataset's public presentation. Constrained to the frontend's controlled authoring catalog; must not accept arbitrary strings, hex colors, or raw CSS.",
      "properties": {
        "preset": {
          "type": "string",
          "enum": [
            "atlas-green",
            "ocean-blue",
            "violet-insight",
            "amber-signal",
            "slate-ops",
            "rose-review",
            "teal-flow",
            "indigo-lab",
            "graphite",
            "citrus",
            "coral",
            "skyline",
            "plum",
            "neutral-light",
            "midnight-cyan",
            "emerald-noir",
            "solar-flare",
            "electric-magenta",
            "neon-lime",
            "aurora",
            "deep-sea",
            "desert-sand",
            "forest-moss",
            "ice-blue",
            "crimson-night",
            "copper-circuit",
            "lavender-mist",
            "monochrome-dark",
            "retro-terminal",
            "cyber-neon"
          ],
          "description": "Optional selected theme preset identifier. Constrained to the controlled frontend catalog; arbitrary theme names, colors, and CSS are rejected."
        }
      }
    },
    "inference_presentation": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional reference-only binding from this profile draft to an existing predict view customization record. Field-level presentation hints, grouping, and view copy for the prediction experience are owned entirely by contracts/predict-view-customization.schema.json and must not be redefined here.",
      "properties": {
        "bound_predict_view_id": {
          "type": ["string", "null"],
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "description": "Optional reference to an existing predict view's view_id. Must match a view_id in the predict view registry. Does not carry field hints, groups, or view copy -- those remain owned by contracts/predict-view-customization.schema.json. Null when no predict view is bound."
        }
      }
    },
    "result_card": {
      "description": "Closed Result Card presentation union. Legacy input remains readable only for binary migration.",
      "oneOf": [
        {"$ref": "#/definitions/canonical_result_card"},
        {"$ref": "#/definitions/multiclass_result_card"},
        {"$ref": "#/definitions/continuous_regression_result_card"},
        {"$ref": "#/definitions/legacy_result_card"}
      ]
    },
    "canonical_result_card": {
      "type": "object",
      "required": ["schema_version", "positive_class_probability_label", "predicted_outcome_label", "positive_outcome_copy", "negative_outcome_copy", "model_section_label", "interpretation"],
      "additionalProperties": false,
      "properties": {
        "schema_version": {"const": "binary-result-presentation.v1"},
        "positive_class_probability_label": {"$ref": "#/definitions/result_card_short_copy"},
        "predicted_outcome_label": {"$ref": "#/definitions/result_card_short_copy"},
        "positive_outcome_copy": {"$ref": "#/definitions/result_card_outcome_copy"},
        "negative_outcome_copy": {"$ref": "#/definitions/result_card_outcome_copy"},
        "model_section_label": {"$ref": "#/definitions/result_card_short_copy"},
        "interpretation": {"$ref": "#/definitions/result_card_interpretation"}
      }
    },
    "multiclass_result_card": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema_version", "predicted_class_label", "class_probability_distribution_label", "model_section_label"],
      "properties": {
        "schema_version": {"const": "multiclass-result-presentation.v1"},
        "predicted_class_label": {"$ref": "#/definitions/result_card_short_copy"},
        "class_probability_distribution_label": {"$ref": "#/definitions/result_card_short_copy"},
        "model_section_label": {"$ref": "#/definitions/result_card_short_copy"}
      }
    },
    "continuous_regression_result_card": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema_version", "predicted_value_label", "model_section_label", "decimal_places"],
      "properties": {
        "schema_version": {"const": "continuous-regression-result-presentation.v1"},
        "predicted_value_label": {"$ref": "#/definitions/result_card_short_copy"},
        "model_section_label": {"$ref": "#/definitions/result_card_short_copy"},
        "decimal_places": {"type": "integer", "minimum": 0, "maximum": 6},
        "value_unit_label": {"$ref": "#/definitions/result_card_short_copy"}
      }
    },
    "legacy_result_card": {
      "type": "object",
      "additionalProperties": false,
      "description": "Deprecated read compatibility for historical drafts. New publications canonicalize this object and never persist these keys.",
      "properties": {
        "probability_label": {
          "type": "string",
          "minLength": 1,
          "maxLength": 160,
          "pattern": "\\S",
          "description": "Deprecated label mapped only to positive_class_probability_label."
        },
        "submit_button_label": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "pattern": "\\S",
          "description": "Deprecated, read-only compatibility field for existing drafts. The submit action is now owned by contracts/predict-view-customization.schema.json's view_copy.submit_button_label; new writes must not set this field, and it is retained only as a temporary migration-era read fallback."
        },
        "model_label": {
          "type": "string",
          "minLength": 1,
          "maxLength": 160,
          "pattern": "\\S",
          "description": "Deprecated section copy mapped only to model_section_label; never technical model identity."
        },
        "badge_preset": {
          "type": "string",
          "enum": ["risk"],
          "description": "Optional selected result badge interpretation preset. Constrained to the single preset currently confirmed by the Dataset Admin presentation design; extend only alongside an actual additional confirmed preset."
        },
        "badge_labels": {
          "$ref": "#/definitions/badge_labels"
        }
      }
    },
    "badge_labels": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional curated public-safe labels for result badge levels. Presentation text only.",
      "properties": {
        "high": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "pattern": "\\S",
          "description": "Optional curated label for the high badge level."
        },
        "medium": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "pattern": "\\S",
          "description": "Optional curated label for the medium badge level."
        },
        "low": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "pattern": "\\S",
          "description": "Optional curated label for the low badge level."
        }
      }
    },
    "result_card_short_copy": {
      "type": "string", "minLength": 1, "maxLength": 160, "pattern": "\\S",
      "description": "Plain presentation copy. Consumers render this as text, never interpreted HTML."
    },
    "result_card_outcome_copy": {
      "type": "string", "minLength": 1, "maxLength": 500, "pattern": "\\S",
      "description": "Plain outcome presentation copy. Consumers render this as text, never interpreted HTML."
    },
    "result_card_interpretation": {
      "type": "object", "required": ["preset", "labels"], "additionalProperties": false,
      "properties": {
        "preset": {"const": "risk"},
        "labels": {"$ref": "#/definitions/canonical_badge_labels"}
      }
    },
    "canonical_badge_labels": {
      "type": "object", "required": ["high", "medium", "low"], "additionalProperties": false,
      "properties": {
        "high": {"type": "string", "minLength": 1, "maxLength": 80, "pattern": "\\S"},
        "medium": {"type": "string", "minLength": 1, "maxLength": 80, "pattern": "\\S"},
        "low": {"type": "string", "minLength": 1, "maxLength": 80, "pattern": "\\S"}
      }
    },
    "documentation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["format", "content"],
      "description": "Optional Markdown documentation authored for the dataset's public Documentation tab. The canonical durable authority is the Markdown source string itself -- never generated HTML, an AST, a filesystem path, or an image manifest. Empty content is permitted only as an unpublished/blank authoring value.",
      "properties": {
        "format": {
          "type": "string",
          "enum": ["markdown"],
          "description": "Documentation source format. Only Markdown is supported."
        },
        "content": {
          "type": "string",
          "maxLength": 50000,
          "description": "Markdown source text. May be empty to represent a blank, unauthored authoring state."
        }
      }
    }
  }
}
